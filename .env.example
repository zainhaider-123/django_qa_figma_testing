DJANGO_SECRET_KEY =
DJANGO_DEBUG =

# Optional: override file-tree cache TTL (seconds, default 300)
# FIGMA_FILE_TREE_CACHE_TIMEOUT = 300

# Optional: override frame-image cache TTL (seconds, default 600)
# FIGMA_FRAME_IMAGE_CACHE_TIMEOUT = 600

# Optional: max retry attempts on Figma 429 rate-limit (default 3)
# FIGMA_RATE_LIMIT_MAX_RETRIES = 3

# Optional: exponential backoff base in seconds between 429 retries (default 2)
# FIGMA_RATE_LIMIT_BACKOFF_BASE = 2
